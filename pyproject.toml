[project]
name = "mc"
version = "0.1.0"
readme = "README.md"
requires-python = "==3.12.9"
dependencies = [
]

[dependency-groups]
dev = [
    "ruff>=0.11.2",
]

[tool.ruff]
target-version = "py312"
output-format = "github"

[tool.ruff.lint]
exclude = ["__pycache__"]
